# Machine-readable template — source of truth for 可开发的需求规格说明书
spec_version: "0.1"
id: SPEC-XXX-001
title:
  zh: 模块 · 功能名
  en: Module · Feature
status: draft   # draft | ready
baseline:
  zh: 对照基线一句话
in_scope: []
out_of_scope: []
actors: []
permissions: []
defaults: {}
states:
  lifecycle: []
  labels: {}          # {state_id: {zh, of}} 人读用中文，禁止只写英文 id
  action_labels: {}   # {action_id: {zh}} 业务动作中文名
  value_labels: {}    # {created: {zh: 已创建}} 枚举值中文；也可由 lifecycle 前缀推导
  action_matrix: []   # [{state, action, allowed, zh}]
ui:
  entry:
    zh: 入口说明
  wireframe: |
    ┌─ 页面 ─┐
    │        │
    └────────┘
  controls: []
empty_states:
  example_blocked:
    zh: 「占位」拦截/空态文案原文（与界面一致后替换）
object_ai:
  enabled: false
behaviors:
  - id: B1
    step_id: 1
    name:
      zh: 「占位」主路径第一步
    given:
      zh: 「占位」前置状态
    when:
      zh: 「占位」用户动作
    then:
      zh: 「占位」可观察结果（数据/状态/文案）
acceptance:
  - id: AC-01
    behavior: B1
    zh: 「占位」Given…When…Then… 可验收表述
pending: []   # {id, missing, impact, owner, status}
overview:
  summary:
    zh: 「占位」一段话说清：谁在什么场景做什么，本规格覆盖到哪条边界
  design_principles:
    - zh: 「占位」为什么这样设计（评审时最常被问的取舍写在这里）
  environment_constraints: []   # 部署/依赖/合规等硬约束
# architecture:                 # 架构图（mermaid 源码入机读，随规格防漂）
#   note:
#     zh: 一句话说明调用关系
#   mermaid: |-
#     flowchart LR
#       A["调用方"] --> B["本模块"]
# responsibilities: []          # [{role, zh, owns: [zh], not_owns: [zh]}] 负责/不负责
# data_contracts: []            # [{id, zh, fields: [{name, zh, type, desc}], rules}] 前后端对齐口径
# error_codes: []               # [{code, zh, trigger, retryable, user_copy}] 开发/测试/客服共用
# decisions: []                 # [{id: D-01, question, options, chosen, status: decided|pending, date, note}]
#                               # 未拍板的 decision 在 status=ready 时 FAIL
sources: []   # {id, kind, path, status: registered|superseded}
source_claims: []
  # {id, source_ref, quote_or_summary, evidence, kind, disposition, spec_refs, confidence}
  # disposition: covered | omitted | assumption | conflict | out_of_scope | pending
